name: "not nullable top level field is absent 2"
enabled: true
overallSchema:
  service: |
    type Query {
      foo: String!
      bar: String
    }
underlyingSchema:
  service: |
    type Query {
      foo: String!
      bar: String
    }
query: |
  query {
    foo
    bar
  }
variables: { }
serviceCalls:
  - serviceName: "service"
    request:
      query: |
        query {
          foo
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": null,
        "errors": [
          {
            "message": "Test"
          }
        ],
        "extensions": {}
      }
  - serviceName: "service"
    request:
      query: |
        query {
          bar
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": {
          "bar": "World"
        },
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": null,
    "errors": [
      {
        "message": "Test",
        "locations": [],
        "extensions": {
          "classification": "DataFetchingException"
        }
      }
    ],
    "extensions": {}
  }

name: coerce values basic case
enabled: true
overallSchema:
  service: |
    type Query {
      aField: Int
    }
underlyingSchema:
  service: |
    type Query {
      aField: String
    }
query: |
  query {
    aField
  }
variables: { }
serviceCalls:
  - serviceName: service
    request:
      query: |
        query {
          aField
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": {
          "aField": "1000"
        },
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": {
      "aField": 1000
    }
  }

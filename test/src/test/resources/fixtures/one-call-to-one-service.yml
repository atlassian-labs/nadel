name: one call to one service
overallSchema:
  service: |
    service service {
      type Query {
        foo: String
      }
    }
services:
  service: |
    type Query {
      foo: String
    }
query: |
  query {
    foo
  }
variables: {}
calls:
  - request:
      query: |
        query nadel_2_service {
          foo
        }
      variables: {}
      operationName: nadel_2_service
    # language=JSON
    response: |-
      {
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": {
      "foo": null
    },
    "extensions": {}
  }

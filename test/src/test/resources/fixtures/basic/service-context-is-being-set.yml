name: service context is being set
enabled: true
overallSchema:
  MyService: |
    type Query {
      foo: String
    }
underlyingSchema:
  MyService: |
    type Query {
      foo: String
    }
query: |
  query {
    foo
  }
variables: { }
serviceCalls:
  - serviceName: MyService
    request:
      query: |
        query {
          foo
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": {
          "foo": null
        },
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": {
      "foo": null
    },
    "extensions": {}
  }

name: transforms can copy array value
enabled: true
overallSchema:
  service: |
    type Query {
      foo: Foo
    }
    type Foo {
      ids: [ID]
    }
underlyingSchema:
  service: |
    type Query {
      foo: Foo
    }
    type Foo {
      ids: [ID]
    }
query: |
  query {
    foo {
      ids
    }
  }
variables: { }
serviceCalls:
  - serviceName: service
    request:
      query: |
        query {
          foo {
            ids
          }
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": {
          "foo": {
            "ids": [
              "FOO-ONE",
              "FOO-TWO",
              "FOO-THREE"
            ]
          }
        },
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": {
      "foo": {
        "ids": [
          "FOO-THREE",
          "FOO-ONE",
          "FOO-TWO"
        ]
      }
    },
    "extensions": {}
  }

name: "graphqlcontext-is-present-on-input"
enabled: true
overallSchema:
  MyService: |
    type Query {
      hello(arg : String) : World
    }
    type World {
      id: ID
      name: String
    }
underlyingSchema:
  MyService: |
    type Query {
      hello(arg : String) : World
    }

    type World {
      id: ID
      name: String
    }
query: |
  query {
    hello(arg : "x") {
      name
    }
  }
variables: { }
serviceCalls:
  - serviceName: "MyService"
    request:
      query: |
        query {
          hello(arg : "x") {
            name
          }
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": {
          "hello": null
        },
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": {
      "hello": null
    },
    "extensions": {}
  }

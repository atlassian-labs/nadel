name: date time scalar is passthrough and can be anything technically
enabled: true
overallSchema:
  service: |
    type Query {
      aField: DateTime
    }
    scalar DateTime
underlyingSchema:
  service: |
    type Query {
      aField: DateTime
    }
    scalar DateTime
query: |
  query {
    aField
  }
variables: { }
serviceCalls:
  - serviceName: service
    request:
      query: |
        query {
          aField
        }
      variables: { }
    # language=JSON
    response: |-
      {
        "data": {
          "aField": "KFC Good"
        },
        "extensions": {}
      }
# language=JSON
response: |-
  {
    "data": {
      "aField": "KFC Good"
    }
  }

rootProject.name = "nadel"
include("lib", "test")

// Prefix every project name with nadel
rootProject.children.forEach { project ->
    project.name = project.name == "lib" ? "nadel" : "nadel-${project.name}"
}
